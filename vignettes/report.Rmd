---
title: "report"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{report}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
pkgdown:
  as_is: true
  extension: pdf
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
devtools::load_all()
library(ggplot2)
```

## Detasets
```{r}
catch <- load_data("../inst/extdata/catch.csv")
catch %>%
  plot(type = "l")
```

## Assessment & future projection
```{r}
catch %>%
  assess(coef = 2) %>%
  project(n = 10) %>%
  ggplot(aes(Year, Abundance, color = Type)) +
  geom_line()
```

```{r}
dummy_artifact <- data.frame(foo = 1:10, bar = 11:20)
write.csv(dummy_artifact, "dummy.csv")
```
